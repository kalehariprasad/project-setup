from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is a setup project with cookicutter and dvc',
    author='hari prasad',
    license='',
)
